[pytest]
# Запускать тесты из корня project/, чтобы импорты src.* работали
testpaths = tests
pythonpath = .

# Подробный вывод по умолчанию
addopts = -v --tb=short

# Отключаем предупреждения pytest о deprecated features
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
